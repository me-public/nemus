# Suite Delete

1. In interactive mode:
   ```bash
   nemus suite delete
   ```

2. In non-interactive mode (for agents):
   ```bash
   nemus suite delete --name <suite-name> --yes
   ```

3. Confirm deletion.
